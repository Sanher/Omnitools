# Changelog

## 0.6.3

- Anade un workflow que notifica al repo padre cuando se publica una tag de version.

## 0.6.2

- Convierte el repo a wrapper-source real consumible por tag.
- Mueve `run.sh`, `nginx.conf` y `patches/` a la raiz.
- Anade `wrapper-source.yaml` y `VERSION`.
- Retira la superficie tipo add-on en `omnitools/`.

## 0.6.1

- Ajusta el wrapper para Home Assistant 2026.04.
